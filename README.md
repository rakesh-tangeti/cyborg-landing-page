# CYBORG // 01 Landing Page

A futuristic cyborg-themed responsive landing page.

## Run
Open `index.html` directly in a browser.

## Customize
- Change the title and text in `index.html`.
- Replace `hello@example.com` with your email.
- Colors, typography and animations are controlled in the `<style>` section.
